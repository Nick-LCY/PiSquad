了解一下项目目前的状态

---

我准备改进几个TUI体验：
1. 比较完差异，选择edit file的时候，需要二次确认。这个二次确认我希望取消掉，选择edit file之后就直接打开editor。
2. 这个我不确定能不能实现 - 在逐一比较文件的时候，在界面上增加一个侧边栏，列出当前正在修改哪份文件，接下来还有哪些文件要修改。侧边栏是一个文件树的形式。
3. 在.pi/.pisquad 中增加.gitignore ，把 backup 排除到git追踪之外。
4. 在用editor修改文件的时候，能提供类似git merge的效果，可以对比改动的部分。

我们先讨论一下这几个TUI的需求，然后你再按流程开始修改。

---

文件树侧边栏的主要需求是这样子的，在逐一比对的时候，用户很可能会只修改其中几个，然后剩下的统一保留旧版/使用新版。所以我希望能增加一个总览一样的效果来降低用户的心智负担。你有什么好的提案吗？如果工作量大的话，这版先不做也没问题，可以留到以后做。

---

不太行，我们这版先直接不动这个功能吧，把剩下三个修复做完即可。

---

都一起补充了吧。

---

Task: 根据 code review 反馈修复以下问题。基于当前代码，不要重新开始。

## 修复 1：添加 validate 拦截未解决的 conflict marker

文件：`src/upgrade/decision-defaults.ts` 的 `defaultOpenEditor` 函数

在 `editor()` 调用中添加 `validate` 参数：
```ts
validate: (text: string) => {
  if (text.includes("<<<<<<<") || text.includes(">>>>>>>")) {
    return "Conflict markers are still present — resolve them and save again.";
  }
  return true;
},
```

`@inquirer/editor` 的 `editorTheme.validationFailureMode` 默认是 `'keep'`，会保留用户输入并把状态回退到 `idle`，让用户再编辑一次。

## 修复 2：dotfile / 无扩展名文件的 postfix 优化

文件：`src/upgrade/decision-defaults.ts` 的 `defaultOpenEditor` 函数

改进 postfix 计算逻辑。当前 `extname(relPath) || ".txt"` 对 `.gitignore`、`Makefile` 等返回 `.txt`，丢失语法高亮。

改为：
- 有扩展名 → 用扩展名（如 `.ts`、`.md`）
- 无扩展名但是 dotfile（basename 以 `.` 开头且长度 > 1，如 `.gitignore`）→ 用完整 basename 作为 postfix（如 `.gitignore`）
- 其他无扩展名（如 `Makefile`）→ 用 `.txt`

可以用 `node:path` 的 `basename` + `extname` 组合判断。

## 修复 3：为 defaultOpenEditor 新增回归测试

新建文件：`test/upgrade/decision-defaults.test.ts`

测试以下边界：
- `current` 和 `incoming` 都为空字符串 → marker 仍独立成行
- 文件末尾无换行 → `ensureTrailingNewline` 正确补齐
- 文件无扩展名 → postfix 正确回退为 `.txt`
- dotfile（如 `.gitignore`）→ postfix 使用完整文件名
- 普通扩展名（如 `.ts`）→ postfix 使用 `.ts`
- editor 返回的内容正确传回（正常路径）
- editor 异常 → 返回空字符串（fallback 路径）
- `waitForUserInput` 为 `false`
- `default` 内容包含正确的 conflict marker 格式

测试方式：创建临时 target 和 assets 目录 + fixture 文件，注入 mock 的 `editor`（通过 `buildDefaultDecisionDeps` 的接口... 但当前接口不支持注入 editor prompt）。

检查 `BuildDefaultDecisionDepsOptions` 和 `buildDefaultDecisionDeps` 的接口，如果当前不支持注入 `editor` prompt，有两种方案：
A. 扩展 `BuildDefaultDecisionDepsOptions` 增加 `editorPrompt` 可选项
B. 直接 import `defaultOpenEditor` 并传入 mock 的 `editor`... 但这需要 `defaultOpenEditor` 被导出

检查当前代码结构，选择最轻量的方案让 `defaultOpenEditor` 可测试。如果需要导出函数或扩展接口，就这样做。

测试框架：项目使用 `node --test`（Node.js 原生 test runner），参考 `test/upgrade/decision.test.ts` 的写法。

## 收尾
- 运行 `npm test` 确认全部测试通过（包括新增的）
- 运行 `npm run build` 确认构建成功
- 不要修改 docs/

---

关于.gitignore这一点我们再明确一下。目前该仓库本身的.gitignore是没有问题的。有问题的是pisquad install之后，.pi/.pisquad下缺少针对backup文件夹的.gitignore。你的修复解决的是这个问题吗？

---

可以，现在执行

---

Task: 执行以下修复。最小化改动。

## 问题
`pisquad install` 之后，目标项目的 `.pi/.pisquad/` 下缺少排除 backups 的 `.gitignore`。

## 修复

### 1. 还原根 `.gitignore`
上一轮把 `.pi/.pisquad/` 拆成了两行精确规则。用户确认本仓库的 `.gitignore` 原本没有问题，还原为：
```
.pi/.pisquad/
```
（即回到原来的整目录忽略规则）

先读取当前 `.gitignore` 确认内容后再改。

### 2. `writeState()` 中自动创建嵌套 `.gitignore`
文件：`src/lib/version.ts`

在 `writeState()` 函数中，`ensureDir()` 之后，写入 `state.json` 之前或之后，添加逻辑：

```ts
// 确保 backups 被 git 排除（即使根 .gitignore 被修改也能兜底）
const pisquadGitignore = join(target, ".pi", ".pisquad", ".gitignore");
if (!existsSync(pisquadGitignore)) {
  atomicWriteFile(pisquadGitignore, "backups/\n");
}
```

需要导入 `join` from `node:path`（检查是否已导入），以及 `existsSync` from `node:fs`（检查是否已导入）。

### 3. 同步 assets 模板
检查 `assets/.pi/.pisquad/` 目录是否存在，如果不存在则创建，并在其中添加 `.gitignore` 文件，内容 `backups/`。

### 4. 测试
- 检查 `test/` 下是否有测试覆盖 `writeState`，如有则更新
- 运行 `npm test` 确认全部通过
- 运行 `npm run build` 确认构建成功