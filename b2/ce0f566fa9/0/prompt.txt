帮我创建一个新项目级的skills，用于描述工作规范：
1. 禁止Agent去查阅、编写代码或编写文档，主进程仅允许读取文档内容（docs文件夹）
接着写清楚项目中拥有的各个agent，及agent的用途。

帮我整理成一个清晰的，带渐进式披露的skills

---

不需要特别声明主进程，因为子进程默认不会加载这个skills。

---

行，接着我们对archivist增加一些限制，主要要求为两点：
1. 所有链接必须使用wikilink格式，如[[test/test.md]]
2. 必须保证链接指向对象存在。

另外关于第二点，有什么工具能做到吗？

---

可以用硬阻断，然后相对目录是相对于文档目录。另外双向链接仅限于引用文档内的内容，文档库外的内容，比如代码等，则不要用链接来表示。

---

行，commit一下这些改动