帮我研究一件事情。能不能对subagent进行精细的skill控制？控制subagent只能看到某些skills。不要写代码，先和我讨论。

---

我们用思路A，然后你看一下这个项目下面的subagent extension，可以考虑在这上面改动。

---

改成严格的白名单模式，未写skills字段等价于禁用全部skills。然后先默认扫描.pi目录即可。其他我没有异议。改完这个之后同步帮我更新旧agent，加上docs的skills

---

我重新加载了extensions，你现在测试一下启动一个agent，看看效果如何。

---

Task: 这是一次冒烟测试，请不要修改任何文件。请完成两件事：

1. 先报告：根据你当前收到的系统提示，有哪些 skill 对你可用？把可用 skill 的名字逐一列出来（如果显示"没有可用 skill"也如实说明）。
2. 用 read 读取 docs/current-state.md 的前 30 行，用两三句话概括里面的内容。

完成后按你的输出格式回复。

---

帮我做两个改动：
1. agent scope默认both。
2. 彻底移除确认弹窗，允许LLM直接调用 agent。

---

我reload了，你测试一下？

---

Task: 冒烟测试，不要读任何文件。只回答：根据你当前的系统提示，有哪些 skill 对你可用？列出名字；如果没有就明确说"没有可用 skill"。一句话即可。

---

不用了，我改了一下subagent，给所有agent都加上了project docs的skills。你帮我把所有的改动一起commit了吧。