我准备为这个项目简单编写一下文档。你可以先阅读一下根目录的README了解这个项目，然后看一下assets里面的内容。

---

给docs填写内容，包括项目的现状、计划等。我们可以先来填写现状的部分，你大概总结一下，我看一看，没问题就来填写。

---

todo暂时不需要补充，把readme的部分整理一下填充到项目文档中。我计划把overview.md，current-state.md填充完成。

---

你让agent去写吧，另外agent用deepseek/deepseek-v4-flash这个模型

---

Task: 任务：填充 `docs/architecture/overview.md`

背景：本仓库 pi-squad 的 docs 模板此前是空骨架，现在正在填充本仓库的真实内容。`docs/current-state.md` 已经填好（项目现状看板），现在需要你填充 `docs/architecture/overview.md`。

步骤：
1. 先 `read` 当前 `docs/architecture/overview.md`（看模板结构），以及 `docs/current-state.md`（已填好，与之呼应、不要冲突）。
2. `read` 根目录 `README.md` 和 `README.zh.md` 作为内容来源。
3. 用中文填充 overview.md，保留模板的四个章节（概述 / 架构 / 核心模块 / 关键决策），把 README 中关于系统设计、四层结构、组件职责、协作流、关键决策的内容整理进去。

内容要点（我已读 README 整理，供参考）：
- 概述：pi-squad 是基于 pi 的、文档驱动的多 Agent 开发底座/模板；核心是把主进程变成纯编排者，实际读写委派给隔离上下文的专用 Agent；两种使用方式（pisquad 安装器 / 复制 assets/）。
- 架构：四层（Extensions / Skills / Agents / Docs，自下而上），给出每层的角色与所在目录；协作链"需求→scout 侦察→planner 规划→worker 执行→reviewer 审查→archivist 归档"；分工铁律（主进程只读 docs/，不直接读写代码或文档）。
- 核心模块：三张表——Agents（scout/planner/worker/reviewer/archivist 各自职责与工具）、Skills（project-docs/workflow）、Extensions（subagent/codegraph/entire/wikilink-lint）。直接采用 README 表格内容，勿遗漏。
- 关键决策：列出几条（主进程即编排者、可分发物集中在 assets/、结构即导航+渐进式披露、wikilink 硬约束），并指向 ADR 目录。

硬约束（非常重要，会被 wikilink-lint 扩展自动校验，不合规会被拦截写入）：
1. wikilink 的路径**相对 docs/ 根**计算（不是相对当前文件所在目录）。
2. wikilink 只能指向 docs/ 内**已存在**的文件。本次可安全引用的现有文件：`current-state.md`、`conventions/README.md`、`architecture/decisions/README.md`、`architecture/README.md`、`README.md`、`glossary.md`、`prds/README.md`、`tasks/README.md`。
3. 库外内容（代码路径如 `assets/.pi/extensions/`、npm 包名、外部 URL）用内联代码或普通 Markdown 链接，**绝不要**用 wikilink。
4. ⚠️ 正文里**不要出现任何字面的双方括号占位**（包括用内联代码示意 wikilink 语法时）。lint 正则不区分代码块，会把字面双方括号当成真实链接，因目标不存在而拦截写入。描述 wikilink 语法时请改用纯文字，例如"当 wikilink 目标指向 docs/ 之外或文件不存在时拒绝写入"。

完成后用 write 写入 `docs/architecture/overview.md`，并向我报告：你最终用了哪些 wikilink、分别指向哪个文件，以及是否一次写入成功（若被 lint 拦截，说明报错并修正后重试）。