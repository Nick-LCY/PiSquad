看一下现在的subagent extension。是不是不会记录subagent的思考输出，只会记录工具调用？

---

中间text我在展开了折叠视图之后也看不到是正常的吗？

---

thinking不需要展示，确保展开视图中能展示中间text就可以。

---

你跑一个我们测试一下

---

Task: 读取项目根目录的 README.md 和 pisuid 两个文件。要求：
1. 先用一两句话口头说明你打算怎么调查、准备读哪些文件（必须先输出这段文字，再调用工具）。
2. 读取这两个文件。
3. 用一句话概括每个文件的用途。
4. 最后给出最终结构化报告。

---

commit一下，然后我们来看另外一个问题：目前agents的frontmatter处都定死了模型，我希望这个工具也能允许在运行时修改模型，frontmatter等于只是一个default 设置。