commit。

---

我准备为这个项目建一个GitHub仓库，用一句简短的话来介绍这个repo，你会怎么介绍？用英文。

---

顺便一提，仓库名字你觉得叫什么好？

---

可以，我用pi squad了。接下来我准备撰写一份readme，中英文版本都需要。我们先来撰写中文的版本（README.zh.md

---

可以，我用pi squad了。接下来我准备撰写一份readme，中英文版本都需要。我们先来撰写中文的版本（README.zh.md）

---

Task: 为撰写一份公开 README 做事实摸底。请返回一份结构化快照，供下游写作使用——不要泛泛而谈，要基于真实文件内容。

## 需要确认的内容

### 1. 仓库根目录
- `ls` 根目录，列出所有文件/目录。
- 是否已存在 `README.md` / `README.zh.md` / `LICENSE` / `package.json` / `.gitignore`？分别报告（有/无）。
- `.pi/` 下的顶层结构（agents/ skills/ extensions/ 各有哪些）。

### 2. 扩展（`.pi/extensions/`）——重点
对以下每个扩展，读其 `index.ts`，给出准确的 1–3 句「用途」+「注册了什么（工具名 / 命令 / 事件钩子）」：
- **entire**（我尚未读过，请重点详读：它到底做什么？对话↔commit 关联？rewind？注册了哪些工具/命令？）
- **codegraph**（确认它包装的是什么 CLI、注册了哪些工具名，例如 explore/node/query/callers 等）
- **subagent**（确认注册的工具名与模式：single/parallel/chain）
- **wikilink-lint**（确认：tool_call 钩子，对 docs/**/*.md 的 write/edit 硬阻断）

### 3. Agent（`.pi/agents/`）
列出每个 agent 的：name、frontmatter 里的 tools、一句话职责。

### 4. Skills（`.pi/skills/`）
列出每个 skill 的 name + description。

### 5. docs/
确认 docs/ 的完整文件树（README.md / current-state.md / glossary.md / architecture/ / conventions/ / prds/ / tasks/）。

## 输出格式
按上述 1–5 分节，每节给清单 + 关键代码摘录（entire/codegraph 的注册部分务必贴关键代码）。最后给一句"这个仓库本质上是什么"的总结。

---

是的，license也是我添加的。然后我更新了文档中的网页链接，你编写的链接是错误的。可以创建对应的英文README了

---

Task: 在仓库根目录创建英文 `README.md`，作为现有 `README.zh.md` 的忠实英文对应版本。

## 严格范围
- **只创建 `README.md` 这一个文件**。不要创建、修改、删除任何其他文件（特别是不许碰 LICENSE、不许改 README.zh.md、不许动 docs/ 或 .pi/）。
- 完成后只汇报 README.md 的事。

## 怎么做
先 `read` 仓库根目录的 `README.zh.md`，然后产出一份**忠实、自然的英文翻译**，保持完全相同的章节顺序、表格、代码块与数据。不要增删章节、不要改写数据。

## 必须遵守的覆盖项（不要沿用中文版里可能的旧值）
- 标题下标语用这句（英文）：
  > A drop-in pi base for multi-agent, doc-driven development.
- pi 的链接统一用 `https://pi.dev/`（中文版已是这个，保持一致）。
- Entire CLI 的链接统一用 `https://entire.io`。
- 标题下方加一行回链：`[中文 README](./README.zh.md)`（对应中文版里的 `[English README](./README.md)`）。
- 许可证节：`[MIT](./LICENSE) — Copyright (c) 2026 Nick Lin.`
- 「工作原理」里的流程链按语义翻译，例如：
  `Requirement → scout recon → planner plans → worker executes → reviewer reviews → archivist archives`

## 风格
- 自然、专业的英文；代码 / 命令 / 路径 / 工具名保持原样（英文）。
- 不要使用 wikilink `[[...]]`（README 在根目录，用标准 Markdown 即可）。
- 不要编造任何额外外链；除上述 pi.dev / entire.io / ./LICENSE / ./README.zh.md 外，不要加其它 URL。

## 完成后汇报
- 文件已创建。
- 列出文中所有外部链接（让我核对只有 pi.dev / entire.io / ./LICENSE / ./README.zh.md 这几类）。
- 确认未改动任何其它文件。